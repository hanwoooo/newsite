test는 코드를 작동시키는 루틴이다.
test를 하는 이유는 지금까지의 코드가 잘돌아가는지 버그는 없는지 
다른 기능들을 추가 하기에 앞서 이 코드가 정상적으로 실행 되는지 알아야되기 때문이다.

또한 코드를 만들었을 때 특정 시점(코드)까지 개발자의 의도대로 실행이 되는지 알 수 있다. 

이러한 코드 test를 자동화 test로 만들어 단 몇초이면 알 수 있게 한다면 그만큼 편한 것이 없을 것이다.